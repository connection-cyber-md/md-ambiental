// Movido para @/components/ui/KpiCard (usado também no Portal do Cliente,
// não é mais exclusivo do admin). Mantido como re-export pra não quebrar
// import antigo, se sobrar algum.
export { KpiCard } from "@/components/ui/KpiCard";
