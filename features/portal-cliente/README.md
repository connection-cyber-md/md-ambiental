# Portal do Cliente (reservado)

Não implementado nesta fase. Vai conter: histórico de coletas, download de CCOs/MTRs, agendamento de novas retiradas — consumindo as tabelas `collections` e `documents` já modeladas em `supabase/migrations/`, filtradas por `company_id` do usuário logado.

O placeholder atual da rota vive em `app/portal/page.tsx`.
