# Motor de Conformidade (reservado)

Não implementado nesta fase. Vai cruzar `companies.ibge_code`/`address_uf` com `regulatory_matrix` (já modelada) para determinar documentos obrigatórios e bloquear operações quando a licença (`license_expiry_date`) estiver vencida.
