# Módulo BPO (reservado)

Não implementado nesta fase. Vai automatizar rotinas por departamento (Comercial, Operacional, Administrativo, Financeiro, RH) usando a tabela `bpo_tasks` já modelada em `supabase/migrations/`.
