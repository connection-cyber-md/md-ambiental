# Painel Operacional (reservado)

Não implementado nesta fase. Vai conter: planejamento de rotas, atribuição de motoristas/veículos, acompanhamento de coletas em tempo real — consumindo `collections`, `vehicles` e `drivers`.

O placeholder atual da rota vive em `app/operacional/page.tsx`.
