# App Mobile-Web do Motorista (reservado)

Não implementado nesta fase. Vai conter: rotas do dia, registro de volume coletado (litros), captura de assinatura digital na tela — consumindo `collections` filtradas por `driver_id`.

O placeholder atual da rota vive em `app/motorista/page.tsx`.
